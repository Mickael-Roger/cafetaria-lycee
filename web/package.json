{
  "name": "cafetaria-web-tests",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:browser": "node --test tests/browser.cjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
